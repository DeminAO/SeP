<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrossMessenger.Client.Messeging.Views.DialogsViewPage"
			 xmlns:vm="clr-namespace:CrossMessenger.Client.Messeging.ViewModels"
			 xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" x:DataType="vm:DialogsViewPageViewModel"
			 prism:ViewModelLocator.AutowireViewModel="True" >

	<ListView x:Name="MyListView"
            ItemsSource="{Binding Contacts}"
            CachingStrategy="RecycleElement">

		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell x:DataType="vm:Contact">
					<Grid Margin="5">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Source="{Binding Path}" HeightRequest="50"/>
						<Label Grid.Column="1" Grid.Row="0" Text="{Binding Name}" Style="{DynamicResource ListItemTextStyle}"/>
						<Label Grid.Column="1" Grid.Row="1" Text="{Binding LastMessage}" Style="{DynamicResource ListItemDetailTextStyle}"/>
					</Grid>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>
